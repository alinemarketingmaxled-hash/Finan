{
  "name": "maxled-financeiro",
  "private": true,
  "type": "module",
  "description": "Painel financeiro Max Led — site estático; dependências só para o middleware/API de autenticação no Vercel (contas, sessões).",
  "dependencies": {
    "@vercel/functions": "^1.6.0",
    "pg": "^8.22.0"
  }
}
